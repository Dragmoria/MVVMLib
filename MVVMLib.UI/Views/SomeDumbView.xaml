<UserControl x:Class="MVVMLib.UI.Views.SomeDumbView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMLib.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock>HEY IK BEN SOME DUMB VIEW</TextBlock>
        <TextBlock Text="{Binding Count}"/>
        <Button Command="{Binding GoBackCommand}">GoBack</Button>
        <Button Command="{Binding GoForwardCommand}">GoForward</Button>
        <Button Command="{Binding NavigateCommand}">To Some View Model</Button>
    </StackPanel>
</UserControl>
